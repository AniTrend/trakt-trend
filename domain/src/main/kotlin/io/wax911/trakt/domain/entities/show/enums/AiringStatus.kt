package io.wax911.trakt.domain.entities.show.enums

enum class AiringStatus(val key: String) {
    ENDED("ended"),
    RETURNING("returning"),
    CANCELED("canceled"),
    IN_PRODUCTION("inproduction");
}